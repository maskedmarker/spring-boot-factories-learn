# web server
server:
  port: 9080
  servlet:
    context-path: /myapp

# spring
spring:
  application:
    name: hello-factories

# logging
logging:
  level:
    root: INFO
    org:
      springframework: warn

configLocation: biz.properties